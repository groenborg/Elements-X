(function () {
    var app = angular.module('EX.constants', []);

    /**
     * The account_id of the bar is 4, if that changes, this needs to be changed as well or
     * the application will break
     * */
    app.value('bar', 4);


})();

